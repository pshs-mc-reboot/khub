// This file is an example of functions in TypeScript.

// function declaration
function add_one(x: number): number {
    return x + 1
}

// function expression
var add_two = function(x: number): number {
    return x + 2
}

// arrow function
var add_three = (x: number): number => {
    return x + 3
}
